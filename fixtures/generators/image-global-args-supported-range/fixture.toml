schema = 1
id = "image-global-args-supported-range"
suite = "generators"
description = "Records target Image GlobalArgs reset, tokenization, malformed-line omission, and pre-pull command placement without importing those semantics into QuadletLens."
secrets_reviewed = true
files = ["image-global-args.image", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "Dry-run generator only; no image pull or runtime execution."

[expectations]
summary = "The target resets earlier values, tokenizes/unquotes/C-unescapes later values, omits a malformed physical line, and places ordered retained tokens between podman and image pull; QuadletLens preserves authored physical lines."
