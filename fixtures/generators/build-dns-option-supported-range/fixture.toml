schema = 1
id = "build-dns-option-supported-range"
suite = "generators"
description = "Exercises reset-aware opaque Build DNSOption values across the recorded Podman range."
secrets_reviewed = true
files = ["build-dns-option.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness invokes only Podman's dry-run Quadlet generator; it does not resolve names, read host DNS or resolv.conf, create a network, or execute a build."

[expectations]
summary = "Every recorded Podman release emits only ordered separate --dns-option ndots:1 then --dns-option use-vc pairs before final '.', with reset behavior evidenced by no rotate or empty output."
