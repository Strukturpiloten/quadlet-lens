schema = 1
id = "volume-gid-supported-range"
suite = "generators"
description = "Records target Volume GID support boundary without importing GID semantics into QuadletLens."
secrets_reviewed = true
files = ["volume-gid.volume", "expected-fragments.txt"]
[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"
[environment]
description = "Dry-run generator only; no volume creation or runtime execution."
[expectations]
summary = "Podman before 6.0 rejects GID; 6.0.x emits --gid 5678 before the volume name."
