schema = 1
id = "volume-group-supported-range"
suite = "generators"
description = "Records target numeric Volume Group command construction without importing group or ownership semantics into QuadletLens."
secrets_reviewed = true
files = ["volume-group.volume", "expected-fragments.txt"]
[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"
[environment]
description = "Dry-run generator only; no volume operation or runtime execution."
[expectations]
summary = "The target emits one o=gid=456 option before the volume name; QuadletLens preserves the opaque authored singleton."
