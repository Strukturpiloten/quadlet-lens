schema = 1
id = "volume-uid-supported-range"
suite = "generators"
description = "Records target Volume UID support boundary without importing UID semantics into QuadletLens."
secrets_reviewed = true
files = ["volume-uid.volume", "expected-fragments.txt"]
[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"
[environment]
description = "Dry-run generator only; no volume creation or runtime execution."
[expectations]
summary = "Podman before 6.0 omits UID; 6.0.x emits --uid 1234 before the volume name."
