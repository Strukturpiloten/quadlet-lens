schema = 1
id = "container-environment-reset-supported-range"
suite = "generators"
description = "Records Container Environment reset command construction without adding Environment parsing or target-effective-value normalization to QuadletLens."
secrets_reviewed = true
files = ["environment-reset.container", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "Dry-run generator only; no manager environment inspection, container workload, or runtime execution."

[expectations]
summary = "The target reset removes the two distinct pre-reset assignments and emits each distinct post-reset assignment as a separate --env argument."
