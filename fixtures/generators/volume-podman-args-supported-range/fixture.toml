schema = 1
id = "volume-podman-args-supported-range"
suite = "generators"
description = "Records target Volume PodmanArgs reset, tokenization, malformed-line omission, and terminal volume-create placement without importing those semantics into QuadletLens."
secrets_reviewed = true
files = ["volume-podman-args.volume", "expected-fragments.txt"]
[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"
[environment]
description = "Dry-run generator only; no volume creation or runtime execution."
[expectations]
summary = "The target resets earlier values, tokenizes/unquotes/C-unescapes later values, omits a malformed physical line, and places ordered retained tokens at the end of volume create before the volume name; QuadletLens preserves authored physical lines."
