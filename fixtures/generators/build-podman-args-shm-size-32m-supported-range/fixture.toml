schema = 1
id = "build-podman-args-shm-size-32m-supported-range"
suite = "generators"
description = "Records one exact opaque Build PodmanArgs --shm-size=32m command argument across Podman 5.4.0 through 6.1.0."
secrets_reviewed = true
files = ["build-podman-args-shm-size-32m.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness mounts this directory read-only and invokes only Podman's dry-run Quadlet generator. It does not add a Build ShmSize key; establish Compose or unit equivalence; infer zero or omission defaults; select IPC; inspect host, cgroup, or memory behavior; execute a build or workload; or establish runtime or conversion behavior."

[expectations]
summary = "Every recorded generator emits one equals-form --shm-size=32m argument immediately before final positional '.'. It rejects separate, quoted, alternate, duplicate, and reordered spellings and proves command text only."
