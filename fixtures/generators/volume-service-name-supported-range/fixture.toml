schema = 1
id = "volume-service-name-supported-range"
suite = "generators"
description = "Records target Volume ServiceName selection and generated-unit naming boundaries without importing identity semantics into QuadletLens."
secrets_reviewed = true
files = ["service-name-default.volume", "service-name-override.volume", "service-name-template@.volume", "service-name-unmatched-quote.volume", "expected-fragments.txt"]
[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"
[environment]
description = "Dry-run generator only; no volume operation, systemd operation, or runtime execution."
[expectations]
summary = "The target derives ordinary and template defaults, selects the last ServiceName, appends .service, and has version-scoped template and unmatched-quote lookup behavior; QuadletLens preserves source text."
