schema = 1
id = "build-podman-args-cache-locations-supported-range"
suite = "generators"
description = "Records exact opaque Build PodmanArgs cache-location command text across Podman 5.4.0 through 6.0.2."
secrets_reviewed = true
files = ["build-podman-args-cache-locations.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness mounts this directory read-only and invokes only Podman's dry-run Quadlet generator. It forwards command text only; it does not use a cache, resolve contexts, paths, registries, images, credentials, or services, validate the Podman CLI, build an image, or start a workload."

[expectations]
summary = "Every recorded generator emits one ordered terminal --cache-from/--cache-to chain before final positional '.'. It rejects equals, quoted, missing, duplicate, and reordered spellings and proves forwarding only, not effective cache use."
