schema = 1
id = "build-podman-args-add-host-buildhost-supported-range"
suite = "generators"
description = "Records one exact opaque Build PodmanArgs --add-host=buildhost:192.0.2.10 command argument across Podman 5.4.0 through 6.0.2."
secrets_reviewed = true
files = ["build-podman-args-add-host-buildhost.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness mounts this directory read-only and invokes only Podman's dry-run Quadlet generator. It does not lower Compose list or map forms; establish IPv6 or host-gateway equivalence; alter DNS or /etc/hosts; resolve conflicts or defaults; execute a build; establish runtime behavior; or establish conversion behavior."

[expectations]
summary = "Every recorded generator emits one equals-form --add-host=buildhost:192.0.2.10 argument immediately before final positional '.'. It rejects separate, quoted, alternate, duplicate, and reordered spellings and proves command text only."
