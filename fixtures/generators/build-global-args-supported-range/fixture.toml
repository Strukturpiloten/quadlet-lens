schema = 1
id = "build-global-args-supported-range"
suite = "generators"
description = "Records target Build GlobalArgs reset, tokenization, malformed-line omission, and pre-build command placement without importing those semantics into QuadletLens."
secrets_reviewed = true
files = ["build-global-args.build", "expected-fragments.txt"]
[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"
[environment]
description = "Dry-run generator only; no build or runtime execution."
[expectations]
summary = "The target resets earlier values, tokenizes/unquotes/C-unescapes later values, omits a malformed physical line, and places ordered retained tokens between podman and build; QuadletLens preserves authored physical lines."
