schema = 1
id = "build-podman-args-ulimit-nproc-supported-range"
suite = "generators"
description = "Records one exact opaque Build PodmanArgs --ulimit=nproc=4096:8192 command argument across Podman 5.4.0 through 6.1.0."
secrets_reviewed = true
files = ["build-podman-args-ulimit-nproc.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness mounts this directory read-only and invokes only Podman's dry-run Quadlet generator. It does not establish Compose name, range, or -1 equivalence; host/rootless/rootful, RUN, cgroup, or default behavior; build execution; runtime resource-limit enforcement; or conversion behavior."

[expectations]
summary = "Every recorded generator emits one equals-form --ulimit=nproc=4096:8192 argument immediately before final positional '.'. It rejects separate, quoted, alternate, duplicate, and reordered spellings and proves command text only."
