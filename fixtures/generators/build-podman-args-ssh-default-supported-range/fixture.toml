schema = 1
id = "build-podman-args-ssh-default-supported-range"
suite = "generators"
description = "Records one exact opaque Build PodmanArgs --ssh=default command argument across Podman 5.4.0 through 6.1.0."
secrets_reviewed = true
files = ["build-podman-args-ssh-default.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness mounts this non-secret fixture directory read-only and invokes only Podman's dry-run Quadlet generator. It does not provide, resolve, inspect, or use keys, sockets, an agent, PEM data, paths, environments, mounts, builds, runtime state, or Compose lowering."

[expectations]
summary = "Every recorded generator emits one equals-form --ssh=default argument immediately before final positional '.'. It rejects separate, quoted, alternate, duplicate, and reordered spellings and proves command text only."
