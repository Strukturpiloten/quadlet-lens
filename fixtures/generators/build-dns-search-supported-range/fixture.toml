schema = 1
id = "build-dns-search-supported-range"
suite = "generators"
description = "Exercises reset-aware opaque Build DNSSearch values across the recorded Podman range."
secrets_reviewed = true
files = ["build-dns-search.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness invokes only Podman's dry-run Quadlet generator; it does not resolve domains, inspect DNS or resolver state, create a network, or execute a build."

[expectations]
summary = "Every recorded Podman release emits only ordered separate --dns-search corp.example then --dns-search . pairs before final '.', with reset behavior evidenced by no old or empty output."
