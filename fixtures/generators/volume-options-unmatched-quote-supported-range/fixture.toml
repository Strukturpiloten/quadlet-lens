schema = 1
id = "volume-options-unmatched-quote-supported-range"
suite = "generators"
description = "Records the exact Podman unmatched-quote boundary for singleton Volume Options."
secrets_reviewed = true
files = ["expected-fragments.txt", "options-unmatched-quote.volume"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness mounts this directory read-only and invokes only exact Podman Quadlet generators in dry-run mode. It does not create a volume or inspect a mount."

[expectations]
summary = "Every release accepts the unmatched Options quote, but Podman 5.4.0 through 5.8.1 strips it while Podman 5.8.2 through 6.1.0 preserves it in the generated `o=` argument presentation."
