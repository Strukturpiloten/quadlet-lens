schema = 1
id = "build-podman-args-cap-add-cap-sys-admin-supported-range"
suite = "generators"
description = "Records one exact opaque Build PodmanArgs --cap-add=CAP_SYS_ADMIN command argument across Podman 5.4.0 through 6.0.2."
secrets_reviewed = true
files = ["build-podman-args-cap-add-cap-sys-admin.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness mounts this directory read-only and invokes only Podman's dry-run Quadlet generator. It does not establish Compose entitlement equivalence or conversion; actual capability grants; build execution; LSM, seccomp, rootless, or runtime effects."

[expectations]
summary = "Every recorded generator emits one equals-form --cap-add=CAP_SYS_ADMIN argument immediately before final positional '.'. It rejects separate, quoted, alternate, duplicate, and reordered spellings and proves command text only."
