schema = 1
id = "build-podman-args-isolation-chroot-supported-range"
suite = "generators"
description = "Records one exact opaque Build PodmanArgs --isolation=chroot command argument across Podman 5.4.0 through 6.0.2."
secrets_reviewed = true
files = ["build-podman-args-isolation-chroot.build", "expected-fragments.txt"]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "The harness mounts this directory read-only and invokes only Podman's dry-run Quadlet generator. It does not select an isolation mode, interpret defaults, environments, namespaces, rootless/rootful operation, LSM policy, paths, images, or services, validate the Podman CLI, build an image, or start a workload."

[expectations]
summary = "Every recorded generator emits one equals-form --isolation=chroot argument immediately before final positional '.'. It rejects separate, quoted, alternate, duplicate, and reordered spellings and proves command text only."
