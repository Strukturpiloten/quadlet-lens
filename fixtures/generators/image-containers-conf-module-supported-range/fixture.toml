schema = 1
id = "image-containers-conf-module-supported-range"
suite = "generators"
description = "Records target Image ContainersConfModule reset and ordered base-command construction without importing target lookup semantics into QuadletLens."
secrets_reviewed = true
files = ["image-containers-conf-module.image", "expected-fragments.txt"]
[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"
[environment]
description = "Dry-run generator only; placeholder module paths are never read and no image pull or runtime execution occurs."
[expectations]
summary = "The target resets logical values at an empty assignment and emits only ordered post-reset --module arguments before image pull; QuadletLens preserves authored physical lines."
