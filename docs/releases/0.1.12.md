# QuadletLens 0.1.12

- Adds evidence-scoped generic `PodmanArgs`, container logging/network identity, network, and volume keys across Podman 5.4.0–6.0.2; this is dry-run command-text evidence only, not runtime or cross-format equivalence.
- Consolidates opaque `.build` support including singleton `AuthFile`/`IgnoreFile` and repeatable `BuildArg`/`Secret`/`GroupAdd`/`DNS`/`DNSOption`/`DNSSearch`/`Annotation`/`PodmanArgs` with finite dry-run command-text evidence only; it does not claim runtime or cross-format behavior.
- Retains exact 20-release capability evidence, including the 5.6.0 network-label bare-token and 6.0.0 volume-options device-prerequisite boundaries.
