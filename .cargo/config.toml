[alias]
ci-check = "check --locked --workspace --all-targets --all-features"
ci-catalogue = "test --locked --test capabilities"
ci-clippy = "clippy --locked --workspace --all-targets --all-features -- -D warnings"
ci-doc = "doc --locked --workspace --all-features --no-deps"
ci-doctest = "test --locked --doc --workspace --all-features"
ci-generators = "test --locked --test generators -- --ignored --nocapture"
ci-model = "test --locked --test model --test document_sets --test render"
ci-package = "package --locked"
ci-policy = "test --locked --test repository_policy --test specification_inventory"
ci-real-world-quadlet = "test --locked --test real_world_quadlet -- --ignored --nocapture"
ci-test = "test --locked --workspace --all-targets --all-features"
